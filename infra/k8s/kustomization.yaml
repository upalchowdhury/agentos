apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: agentos

resources:
  - namespace.yaml
  - secrets.yaml

commonLabels:
  app.kubernetes.io/part-of: agent-economy-os
  app.kubernetes.io/managed-by: kustomize
